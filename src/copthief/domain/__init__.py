"""Pure game domain: board geometry, rules, scoring and the subgame state machine.

This package has no I/O, no networking and no LLM dependency, so it is fully
unit-testable and reusable across the orchestrator, the MCP servers and the GUI.
"""
